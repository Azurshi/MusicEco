<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MusicEco.Views.Widgets.LabelButton">
    <Label x:Name="InnerLabel"
           MaxLines="1"
           LineBreakMode="TailTruncation"
           HorizontalTextAlignment="Start"
           VerticalTextAlignment="Center"
           HorizontalOptions="Fill"
           VerticalOptions="Fill"
           BackgroundColor="Transparent"
           FontSize="Micro"
           Margin="4, 0"
           TextColor="{StaticResource SlotText}">
        <Label.GestureRecognizers>
            <TapGestureRecognizer Tapped="OnClicked"/>
            <PointerGestureRecognizer PointerPressed="OnPressed"
                                      PointerReleased="OnReleased"
                                      PointerEntered="OnEntered"
                                      PointerExited="OnExited"/>
        </Label.GestureRecognizers>
    </Label>
</ContentView>
