<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:service="clr-namespace:MusicEco.Services"
             xmlns:pageVM="clr-namespace:MusicEco.ViewModels.Pages"
             xmlns:itemVM="clr-namespace:MusicEco.ViewModels.Items"
             xmlns:item="clr-namespace:MusicEco.Views.Items"
             xmlns:control="clr-namespace:MusicEco.Views.Controls"
             xmlns:button="clr-namespace:MusicEco.Views.Buttons"
             xmlns:commands="clr-namespace:MusicEco.Views.Commands"
             x:Class="MusicEco.Views.Pages.AlbumDetailPage"
             x:DataType="pageVM:AlbumDetailPageViewModel">
    <ContentView.Resources>
        <ResourceDictionary>
            <DataTemplate x:Key="MenuTemplate"
                          x:DataType="itemVM:AudioEntryViewModel">
                <VerticalStackLayout>
                    <button:MenuItemButton Text="{Binding [Menu_AddToQueue], Source={Static service:Localization.L}}"
                                           Command="{Static commands:MenuCommands.AddToQueueCommand}" />
                    <button:MenuItemButton Text="{Binding [Menu_AddToPlaylist], Source={Static service:Localization.L}}"
                                           Command="{Static commands:MenuCommands.AddToPlaylistCommand}" />
                    <button:MenuItemButton Text="{Binding [Menu_Info], Source={Static service:Localization.L}}"
                                           Command="{Static commands:MenuCommands.ShowAudioInfoCommand}" />
                </VerticalStackLayout>
            </DataTemplate>
            <DataTemplate x:Key="ListAudioItemTemplate"
                          x:DataType="itemVM:AudioEntryViewModel">
                <item:ItemFrame HeightRequest="{DynamicResource IconItemSize}"
                                Command="{Binding SelectItemCommand, Source={RelativeSource AncestorType={x:Type pageVM:AlbumDetailPageViewModel}}}">
                    <item:ItemFrame.ItemContent>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <control:ManagedSKIcon Grid.Column="0"
                                                 WidthRequest="{DynamicResource IconItemSize}"
                                                 FileHash="{Binding FileHash}"
                                                 Option="Small" />
                            <Label Grid.Column="1"
                                   Text="{Binding DisplayTitle}"
                                   Style="{StaticResource OneLine}" />
                            <item:MenuButton Grid.Column="2"
                                             WidthRequest="{DynamicResource IconButtonItemSize}"
                                             MenuTemplate="{StaticResource MenuTemplate}" />
                        </Grid>
                    </item:ItemFrame.ItemContent>
                </item:ItemFrame>
            </DataTemplate>
            <DataTemplate x:Key="GridAudioItemTemplate"
                          x:DataType="itemVM:AudioEntryViewModel">
                <item:ItemFrame HeightRequest="{DynamicResource GridItemSize}"
                                Command="{Binding SelectItemCommand, Source={RelativeSource AncestorType={x:Type pageVM:AlbumDetailPageViewModel}}}">
                    <item:ItemFrame.ItemContent>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <control:ManagedSKIcon Grid.Row="0"
                                                 FileHash="{Binding FileHash}"
                                                 Option="Medium" />
                            <Label Grid.Row="1"
                                   Text="{Binding DisplayTitle}"
                                   HeightRequest="{DynamicResource TextOnlySize}"
                                   Style="{StaticResource OneLine}" 
                                   HorizontalTextAlignment="Center"/>
                            <item:MenuButton Grid.RowSpan="2"
                                             WidthRequest="{DynamicResource IconButtonItemSize}"
                                             HeightRequest="{DynamicResource IconButtonItemSize}"
                                             MenuTemplate="{StaticResource MenuTemplate}" />
                        </Grid>
                    </item:ItemFrame.ItemContent>
                </item:ItemFrame>
            </DataTemplate>
        </ResourceDictionary>
    </ContentView.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0"
              ColumnSpacing="8"
              HeightRequest="{DynamicResource HeaderSize}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <button:NavigationBackwardButton Grid.Column="0"
                                             WidthRequest="{DynamicResource HeaderSize}"
                                             Margin="4, 8" />
            <Label Grid.Column="1"
                   Text="{Binding AlbumName}"
                   Style="{StaticResource OneLine}" />
            <button:CollectionDisplayModeButtonSimple Grid.Column="2"
                                                      DisplayMode="{Binding DisplayMode, Mode=TwoWay}" />
        </Grid>
        <control:CollectionViewExtend Grid.Row="1"
                                      DisplayMode="{Binding DisplayMode}"
                                      ItemsSource="{Binding Items.Items}">
            <control:CollectionViewExtend.ItemPresets>
                <control:CollectionViewPreset DisplayMode="SimpleList"
                                              ItemTemplate="{StaticResource ListAudioItemTemplate}">
                    <control:CollectionViewPreset.ItemsLayout>
                        <LinearItemsLayout Orientation="Vertical"
                                           ItemSpacing="8"/>
                    </control:CollectionViewPreset.ItemsLayout>
                </control:CollectionViewPreset>
                <control:CollectionViewPreset DisplayMode="SimpleGrid"
                                              ItemTemplate="{StaticResource GridAudioItemTemplate}">
                    <control:CollectionViewPreset.ItemsLayout>
                        <GridItemsLayout Orientation="Vertical"
                                         Span="4"
                                         VerticalItemSpacing="4"
                                         HorizontalItemSpacing="4" />
                    </control:CollectionViewPreset.ItemsLayout>
                </control:CollectionViewPreset>
            </control:CollectionViewExtend.ItemPresets>
            
        </control:CollectionViewExtend>
    </Grid>
</ContentView>
